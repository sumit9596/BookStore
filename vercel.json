{
    "buildCommand": "cd Fronted/my-project && npm install && npm run build",
    "outputDirectory": "Fronted/my-project/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}